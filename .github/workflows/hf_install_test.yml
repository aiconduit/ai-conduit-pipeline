name: HyperFrames Install Test
on:
  workflow_dispatch:
jobs:
  test:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Time npm install hyperframes
        run: |
          time npm install -g hyperframes --ignore-scripts
          hyperframes --version
